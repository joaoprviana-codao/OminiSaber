# Known issues

## Objetivo

Registrar pontos que exigem acompanhamento sem alterar código nesta tarefa.

## Itens observados

- A validação SQL local depende das bibliotecas listadas em `backend/requirements-dev.txt`.
- Existem páginas HTML de scaffolding chamadas `code.html`; o uso pretendido precisa de revisão humana antes de serem movidas.
- O projeto possui schemas SQL modulares e um schema completo; a ordem de uso deve permanecer clara.
